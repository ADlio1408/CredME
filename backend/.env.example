# Copy to .env and set a real value before any shared/public deployment.
# If unset, the API falls back to a published, insecure development key
# (see backend/api.py) — fine for local demos, never for anything shared.
CREDME_API_KEY=changeme
